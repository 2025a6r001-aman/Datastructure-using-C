#include<stdio.h>
#define MAX 100
int queue[MAX], front =-1, rear=-1;
int isFull(){
    return rear == MAX-1;

}
int isEmpty(){
    return front ==-1;

}
void enqueue(int data){
    if(isFull()){
        printf("\nQueue overflow!!!\n");
        return;

    }
    queue[++rear] = data;
    if(front == -1)
       front = rear;
       printf("\n%d inserted in queue!!!\n", data);


}
int dequeue(){
    if(isEmpty()){
        printf("Queue Underflow!!!\n");
        return -1;
    }
    int val=queue[front++];
    if(front>rear)
    front = rear = -1;
    return val;

}
int peek(){

}
void display(){

}
int main(){

}
